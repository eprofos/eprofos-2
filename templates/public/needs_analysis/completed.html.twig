{% extends 'public/base.html.twig' %}

{% block title %}Formulaire déjà complété - EPROFOS{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm border-0">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
                    </div>
                    
                    <h1 class="h2 text-success mb-4">Formulaire déjà complété</h1>
                    
                    <div class="alert alert-info mb-4">
                        <i class="fas fa-info-circle me-2"></i>
                        Vous avez déjà rempli ce formulaire d'analyse des besoins.
                    </div>
                    
                    <div class="card bg-light mb-4">
                        <div class="card-body">
                            <h5 class="card-title">Informations de la demande</h5>
                            <div class="row text-start">
                                <div class="col-md-6">
                                    <p><strong>Type :</strong> 
                                        {% if request.type == 'company' %}
                                            <span class="badge bg-primary">Entreprise</span>
                                        {% else %}
                                            <span class="badge bg-info">Particulier</span>
                                        {% endif %}
                                    </p>
                                    <p><strong>Destinataire :</strong> {{ request.recipientName }}</p>
                                </div>
                                <div class="col-md-6">
                                    {% if request.companyName %}
                                        <p><strong>Entreprise :</strong> {{ request.companyName }}</p>
                                    {% endif %}
                                    <p><strong>Complété le :</strong> 
                                        {% if request.completedAt %}
                                            {{ request.completedAt|date('d/m/Y à H:i') }}
                                        {% else %}
                                            Non disponible
                                        {% endif %}
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <p class="text-muted mb-4">
                        Votre analyse des besoins a été transmise à notre équipe pédagogique. 
                        Nous vous contacterons prochainement pour la suite du processus.
                    </p>
                    
                    <div class="d-flex justify-content-center gap-3">
                        <a href="https://eprofos.com" class="btn btn-outline-primary">
                            <i class="fas fa-home me-2"></i>
                            Retour au site EPROFOS
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}