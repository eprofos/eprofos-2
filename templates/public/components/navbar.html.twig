<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm" data-controller="navbar">
    <div class="container">
        <!-- Brand/Logo -->
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo/eprofos-logo.png') }}" alt="EPROFOS" height="40" class="me-2">
            <div class="d-none d-md-block">
                <div class="fw-bold text-primary">EPROFOS</div>
                <small class="text-muted">École professionnelle de formation</small>
            </div>
        </a>

        <!-- Mobile menu toggle -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation menu -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <!-- Home -->
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active' : '' }}" 
                       href="{{ path('app_home') }}">
                        <i class="fas fa-home me-1"></i>Accueil
                    </a>
                </li>

                <!-- About -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{ app.request.get('_route') starts with 'app_about' ? 'active' : '' }}" 
                       href="#" id="aboutDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-info-circle me-1"></i>À propos
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="aboutDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_about') }}">
                            <i class="fas fa-building me-2"></i>Notre organisme
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_about_approach') }}">
                            <i class="fas fa-cogs me-2"></i>Notre approche
                        </a></li>
                    </ul>
                </li>

                <!-- Formations -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{ app.request.get('_route') starts with 'app_formation' ? 'active' : '' }}" 
                       href="#" id="formationsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-graduation-cap me-1"></i>Formations
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="formationsDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_formations_index') }}">
                            <i class="fas fa-list me-2"></i>Toutes les formations
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><h6 class="dropdown-header">Par catégorie</h6></li>
                        <li><a class="dropdown-item" href="{{ path('app_formations_by_category', {slug: 'techniques'}) }}">
                            <i class="fas fa-laptop-code me-2"></i>Formations techniques
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_formations_by_category', {slug: 'transversales'}) }}">
                            <i class="fas fa-users me-2"></i>Formations transversales
                        </a></li>
                    </ul>
                </li>

                <!-- Services -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{ app.request.get('_route') starts with 'app_service' ? 'active' : '' }}" 
                       href="#" id="servicesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-handshake me-1"></i>Services
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="servicesDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_services_index') }}">
                            <i class="fas fa-list me-2"></i>Tous nos services
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_services_by_category', {slug: 'conseil'}) }}">
                            <i class="fas fa-lightbulb me-2"></i>Conseil & Audit
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_services_by_category', {slug: 'accompagnement'}) }}">
                            <i class="fas fa-user-friends me-2"></i>Accompagnement
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_services_by_category', {slug: 'certifications'}) }}">
                            <i class="fas fa-certificate me-2"></i>Certifications
                        </a></li>
                    </ul>
                </li>

                <!-- Contact -->
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') starts with 'app_contact' ? 'active' : '' }}" 
                       href="{{ path('app_contact_index') }}">
                        <i class="fas fa-envelope me-1"></i>Contact
                    </a>
                </li>

                <!-- CTA Button -->
                <li class="nav-item ms-2">
                    <a class="btn btn-primary btn-sm" href="{{ path('app_contact_index') }}#quote-form">
                        <i class="fas fa-paper-plane me-1"></i>Demander un devis
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Search bar (optional, can be toggled) -->
<div class="bg-light border-bottom d-none" id="search-bar">
    <div class="container py-2">
        <form class="d-flex" role="search" action="{{ path('app_formations_index') }}" method="GET">
            <input class="form-control me-2" type="search" name="search" placeholder="Rechercher une formation..." 
                   aria-label="Search" value="{{ app.request.query.get('search', '') }}">
            <button class="btn btn-outline-primary" type="submit">
                <i class="fas fa-search"></i>
            </button>
        </form>
    </div>
</div>