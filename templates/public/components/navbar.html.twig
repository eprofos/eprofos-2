<nav class="modern-navbar navbar navbar-expand-lg navbar-light bg-white shadow-sm" data-controller="navbar">
    <div class="container">
        <!-- Modern Brand/Logo -->
        <a class="modern-navbar-brand d-flex align-items-center" href="{{ path('public_home') }}">
            <div class="logo-container">
                <div class="logo-text">
                    <span class="brand-name">EPROFOS</span>
                    <span class="brand-tagline">EDUCATION & ONLINE COURSES</span>
                </div>
            </div>
        </a>

        <div class="navbar-actions d-none d-lg-flex align-items-center me-3">
            <button class="action-btn search-btn" type="button" data-bs-toggle="collapse" data-bs-target="#search-bar" aria-expanded="false">
                <i class="fas fa-search"></i>
            </button>
        </div>

        <!-- Mobile menu toggle -->
        <button class="navbar-toggler modern-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <!-- Navigation menu -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto">
                <!-- Home -->
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'public_home' ? 'active' : '' }}"
                       href="{{ path('public_home') }}">
                        Accueil
                    </a>
                </li>

                <!-- About Us -->
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') starts with 'public_about' ? 'active' : '' }}"
                       href="{{ path('public_about') }}">
                        À propos de nous
                    </a>
                </li>

                <!-- Pages -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="pagesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Pages
                    </a>
                    <ul class="dropdown-menu modern-dropdown" aria-labelledby="pagesDropdown">
                        <li><a class="dropdown-item" href="{{ path('public_about') }}">
                            <i class="fas fa-building me-2"></i>Notre organisme
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('public_about_approach') }}">
                            <i class="fas fa-cogs me-2"></i>Notre approche
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('public_services_index') }}">
                            <i class="fas fa-handshake me-2"></i>Nos services
                        </a></li>
                    </ul>
                </li>

                <!-- Our Courses -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{ app.request.get('_route') starts with 'public_formation' ? 'active' : '' }}"
                       href="#" id="coursesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Nos formations
                    </a>
                    <ul class="dropdown-menu modern-dropdown" aria-labelledby="coursesDropdown">
                        <li><a class="dropdown-item" href="{{ path('public_formations_index') }}">
                            <i class="fas fa-list me-2"></i>All Courses
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><h6 class="dropdown-header">By Category</h6></li>
                        <li><a class="dropdown-item" href="{{ path('public_formations_by_category', {slug: 'techniques'}) }}">
                            <i class="fas fa-laptop-code me-2"></i>Technical Training
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('public_formations_by_category', {slug: 'transversales'}) }}">
                            <i class="fas fa-users me-2"></i>Soft Skills
                        </a></li>
                    </ul>
                </li>

                <!-- Contact -->
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') starts with 'public_contact' ? 'active' : '' }}"
                       href="{{ path('public_contact_index') }}">
                        Contact
                    </a>
                </li>
            </ul>

            <!-- Join For Free Button -->
            <div class="navbar-cta d-flex align-items-center">
                <a class="btn btn-join-free" href="{{ path('public_contact_index') }}#quote-form">
                    <i class="fas fa-user-plus me-2"></i>Rejoindre gratuitement
                </a>
            </div>
        </div>
    </div>
</nav>

<!-- Modern Search bar -->
<div class="modern-search-bar bg-light border-bottom collapse" id="search-bar">
    <div class="container py-3">
        <form class="search-form" role="search" action="{{ path('public_formations_index') }}" method="GET">
            <div class="search-input-group">
                <input class="form-control search-input" type="search" name="search"
                       placeholder="Recherchez des cours, sujets ou formateurs..."
                       aria-label="Search" value="{{ app.request.query.get('search', '') }}">
                <button class="btn search-submit-btn" type="submit">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </form>
    </div>
</div>