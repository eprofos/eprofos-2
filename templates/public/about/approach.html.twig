{% extends 'public/base.html.twig' %}

{% block title %}Notre Approche - EPROFOS{% endblock %}

{% block meta_description %}Découvrez la méthodologie EPROFOS : analyse des besoins, conception sur mesure, mise en œuvre et suivi personnalisé pour des formations efficaces et adaptées.{% endblock %}

{% block body %}
    <div class="container py-5">
        {# Hero Section #}
        <div class="row mb-5">
            <div class="col-lg-8 mx-auto text-center">
                <h1 class="display-4 fw-bold mb-4">Notre Approche</h1>
                <p class="lead text-muted mb-4">
                    Chez EPROFOS, nous avons développé une méthodologie éprouvée en 4 étapes pour garantir 
                    l'efficacité et la pertinence de nos formations professionnelles.
                </p>
                <p class="text-muted">
                    De l'analyse de vos besoins au suivi post-formation, chaque étape est pensée 
                    pour maximiser votre montée en compétences et assurer votre réussite professionnelle.
                </p>
            </div>
        </div>

        {# Methodology Steps #}
        <div class="row mb-5">
            <div class="col-12">
                <div class="methodology-timeline">
                    {% set step_colors = ['primary', 'success', 'warning', 'info'] %}
                    {% set step_icons = ['fas fa-search', 'fas fa-drafting-compass', 'fas fa-rocket', 'fas fa-chart-line'] %}
                    {% set step_counter = 0 %}
                    
                    {% for key, step in methodology %}
                        {% set step_counter = step_counter + 1 %}
                        {% set color = step_colors[loop.index0] %}
                        {% set icon = step_icons[loop.index0] %}
                        
                        <div class="methodology-step mb-5">
                            <div class="row align-items-center {{ loop.index is odd ? '' : 'flex-row-reverse' }}">
                                <div class="col-lg-6 mb-4">
                                    <div class="step-content {{ loop.index is odd ? 'text-lg-start' : 'text-lg-end' }}">
                                        <div class="step-number-badge bg-{{ color }} text-white mb-3">
                                            <span class="step-number">{{ step_counter }}</span>
                                        </div>
                                        <h3 class="fw-bold mb-3 text-{{ color }}">
                                            <i class="{{ icon }} me-2"></i>{{ step.title }}
                                        </h3>
                                        <p class="text-muted mb-4">{{ step.description }}</p>
                                        
                                        <div class="step-details">
                                            <h5 class="fw-bold mb-3">Étapes clés :</h5>
                                            <ul class="list-unstyled">
                                                {% for detail in step.steps %}
                                                    <li class="mb-2">
                                                        <i class="fas fa-check-circle text-{{ color }} me-2"></i>
                                                        {{ detail }}
                                                    </li>
                                                {% endfor %}
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="col-lg-6 mb-4">
                                    <div class="step-visual text-center">
                                        <div class="step-icon-container bg-{{ color }} bg-opacity-10 rounded-circle mx-auto mb-3">
                                            <i class="{{ icon }} fa-4x text-{{ color }}"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            {% if not loop.last %}
                                <div class="step-connector text-center my-4">
                                    <i class="fas fa-arrow-down fa-2x text-muted"></i>
                                </div>
                            {% endif %}
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        {# Our Commitment Section #}
        <div class="row mb-5">
            <div class="col-lg-10 mx-auto">
                <div class="card bg-primary text-white border-0">
                    <div class="card-body py-5">
                        <div class="row align-items-center">
                            <div class="col-lg-8">
                                <h3 class="fw-bold mb-3">
                                    <i class="fas fa-handshake me-2"></i>Notre engagement qualité
                                </h3>
                                <p class="mb-4">
                                    Cette approche méthodologique nous permet de garantir des formations 
                                    sur mesure, efficaces et parfaitement adaptées à vos objectifs professionnels.
                                </p>
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-award fa-2x me-3"></i>
                                            <div>
                                                <strong>95% de satisfaction</strong><br>
                                                <small>Taux de satisfaction client</small>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-clock fa-2x me-3"></i>
                                            <div>
                                                <strong>Suivi 6 mois</strong><br>
                                                <small>Accompagnement post-formation</small>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-lg-4 text-center">
                                <img src="{{ asset('images/quality-commitment.svg') }}" 
                                     alt="Engagement qualité" 
                                     class="img-fluid"
                                     style="max-height: 200px;">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {# Benefits Section #}
        <div class="row mb-5">
            <div class="col-12">
                <h2 class="h3 fw-bold text-center mb-5">Les bénéfices de notre approche</h2>
                <div class="row">
                    <div class="col-lg-3 col-md-6 mb-4">
                        <div class="card h-100 text-center border-0 shadow-sm">
                            <div class="card-body">
                                <i class="fas fa-bullseye fa-3x text-primary mb-3"></i>
                                <h5 class="card-title">Formations ciblées</h5>
                                <p class="card-text text-muted">
                                    Programmes parfaitement adaptés à vos besoins spécifiques et objectifs professionnels.
                                </p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-lg-3 col-md-6 mb-4">
                        <div class="card h-100 text-center border-0 shadow-sm">
                            <div class="card-body">
                                <i class="fas fa-user-friends fa-3x text-success mb-3"></i>
                                <h5 class="card-title">Accompagnement personnalisé</h5>
                                <p class="card-text text-muted">
                                    Suivi individualisé tout au long du parcours de formation et au-delà.
                                </p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-lg-3 col-md-6 mb-4">
                        <div class="card h-100 text-center border-0 shadow-sm">
                            <div class="card-body">
                                <i class="fas fa-chart-line fa-3x text-warning mb-3"></i>
                                <h5 class="card-title">Résultats mesurables</h5>
                                <p class="card-text text-muted">
                                    Évaluation continue et mesure de l'efficacité pour garantir l'atteinte des objectifs.
                                </p>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-lg-3 col-md-6 mb-4">
                        <div class="card h-100 text-center border-0 shadow-sm">
                            <div class="card-body">
                                <i class="fas fa-sync-alt fa-3x text-info mb-3"></i>
                                <h5 class="card-title">Amélioration continue</h5>
                                <p class="card-text text-muted">
                                    Ajustements en temps réel et optimisation constante des méthodes pédagogiques.
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {# Call to Action #}
        <div class="row">
            <div class="col-lg-8 mx-auto text-center">
                <div class="card bg-light border-0">
                    <div class="card-body py-5">
                        <h3 class="fw-bold mb-3">Découvrez notre approche en action</h3>
                        <p class="text-muted mb-4">
                            Prêt à bénéficier de notre méthodologie éprouvée ? Contactez-nous pour discuter 
                            de vos besoins et découvrir comment nous pouvons vous accompagner.
                        </p>
                        <div class="d-flex flex-wrap gap-3 justify-content-center">
                            <a href="{{ path('app_formations_index') }}" class="btn btn-primary">
                                <i class="fas fa-graduation-cap me-2"></i>Nos formations
                            </a>
                            <a href="{{ path('app_contact_quote') }}" class="btn btn-success">
                                <i class="fas fa-calculator me-2"></i>Demander un devis
                            </a>
                            <a href="{{ path('app_about') }}" class="btn btn-outline-primary">
                                <i class="fas fa-info-circle me-2"></i>En savoir plus sur EPROFOS
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .methodology-timeline {
            position: relative;
        }
        
        .step-number-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            font-size: 1.5rem;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        
        .step-icon-container {
            width: 150px;
            height: 150px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 3px solid rgba(var(--bs-primary-rgb), 0.2);
        }
        
        .step-connector {
            opacity: 0.6;
        }
        
        .methodology-step {
            position: relative;
        }
        
        .step-content {
            padding: 2rem;
        }
        
        .step-details ul li {
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .step-details ul li:last-child {
            border-bottom: none;
        }
        
        @media (max-width: 991.98px) {
            .step-content {
                text-align: center !important;
                padding: 1rem;
            }
            
            .methodology-step .row {
                flex-direction: column !important;
            }
            
            .step-connector {
                transform: rotate(90deg);
                margin: 2rem 0;
            }
        }
        
        /* Animation on scroll */
        .methodology-step {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease-in-out;
        }
        
        .methodology-step.animate {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        // Animate methodology steps on scroll
        document.addEventListener('DOMContentLoaded', function() {
            const steps = document.querySelectorAll('.methodology-step');
            
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };
            
            const observer = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('animate');
                    }
                });
            }, observerOptions);
            
            steps.forEach(step => {
                observer.observe(step);
            });
        });
    </script>
{% endblock %}