{% extends 'public/base.html.twig' %}

{% block title %}Téléchargement de documents - EPROFOS{% endblock %}

{% block meta_description %}Téléchargez tous les documents légaux et réglementaires d'EPROFOS : règlement intérieur, livret du stagiaire, conditions générales.{% endblock %}

{% block body %}
    {# Modern Hero Section #}
    <section class="hero-section py-5">
        <div class="container">
            <div class="row align-items-center min-vh-50">
                <div class="col-lg-8 mx-auto text-center">
                    <span class="section-badge mb-3">TÉLÉCHARGEMENTS</span>
                    <h1 class="display-4 fw-bold mb-4">
                        Téléchargement de <span class="text-warning">documents</span>
                    </h1>
                    <p class="lead mb-4 opacity-90">
                        Accédez à tous nos documents légaux et réglementaires en téléchargement libre.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <div class="container py-5">
        {# Introduction Section #}
        <div class="row mb-5">
            <div class="col-lg-10 mx-auto">
                <div class="card border-0 shadow-sm" style="border-radius: 25px; background: linear-gradient(135deg, #e8f4f8 0%, #d1ecf1 100%);">
                    <div class="card-body p-5">
                        <div class="text-center mb-4">
                            <div class="d-flex align-items-center justify-content-center mb-3">
                                <div class="bg-info text-white rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 60px; height: 60px;">
                                    <i class="fas fa-download"></i>
                                </div>
                                <h2 class="h3 fw-bold mb-0">Documents disponibles</h2>
                            </div>
                        </div>
                        <p class="text-center mb-0" style="line-height: 1.7; font-size: 1.1rem;">
                            Tous nos documents légaux et réglementaires sont disponibles en téléchargement 
                            libre au format PDF. Ces documents sont régulièrement mis à jour.
                        </p>
                    </div>
                </div>
            </div>
        </div>

        {# Download All Section #}
        {% if published_documents|length > 0 %}
        <div class="row mb-5">
            <div class="col-lg-8 mx-auto">
                <div class="card border-0 shadow-sm" style="border-radius: 25px; background: linear-gradient(135deg, #fff8e1 0%, #ffecb3 100%);">
                    <div class="card-body p-4 text-center">
                        <h3 class="fw-bold mb-3">
                            <i class="fas fa-cloud-download-alt me-2"></i>Téléchargement groupé
                        </h3>
                        <p class="mb-3">
                            Téléchargez tous les documents en une seule fois dans une archive ZIP.
                        </p>
                        <a href="{{ path('app_legal_download_all') }}" 
                           class="btn btn-warning btn-lg" 
                           style="border-radius: 25px;">
                            <i class="fas fa-file-archive me-2"></i>Télécharger tout (ZIP)
                        </a>
                        <p class="small text-muted mt-2 mb-0">
                            {{ published_documents|length }} document{{ published_documents|length > 1 ? 's' : '' }} disponible{{ published_documents|length > 1 ? 's' : '' }}
                        </p>
                    </div>
                </div>
            </div>
        </div>
        {% endif %}

        {# Documents Grid #}
        {% if published_documents|length > 0 %}
        <div class="row">
            {% for document in published_documents %}
            <div class="col-lg-6 mb-4">
                <div class="card border-0 shadow-sm h-100" style="border-radius: 25px;">
                    <div class="card-body p-4">
                        {# Document Icon #}
                        <div class="text-center mb-3">
                            {% if document.type == 'internal_regulation' %}
                                <div class="bg-danger text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px;">
                                    <i class="fas fa-gavel fa-2x"></i>
                                </div>
                            {% elseif document.type == 'student_handbook' %}
                                <div class="bg-primary text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px;">
                                    <i class="fas fa-book fa-2x"></i>
                                </div>
                            {% elseif document.type == 'training_terms' %}
                                <div class="bg-success text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px;">
                                    <i class="fas fa-file-contract fa-2x"></i>
                                </div>
                            {% elseif document.type == 'accessibility_policy' %}
                                <div class="bg-info text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px;">
                                    <i class="fas fa-universal-access fa-2x"></i>
                                </div>
                            {% elseif document.type == 'accessibility_procedures' %}
                                <div class="bg-purple text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px; background-color: #9c27b0;">
                                    <i class="fas fa-clipboard-list fa-2x"></i>
                                </div>
                            {% elseif document.type == 'accessibility_faq' %}
                                <div class="bg-warning text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px;">
                                    <i class="fas fa-question-circle fa-2x"></i>
                                </div>
                            {% else %}
                                <div class="bg-secondary text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 70px; height: 70px;">
                                    <i class="fas fa-file-pdf fa-2x"></i>
                                </div>
                            {% endif %}
                        </div>

                        {# Document Title and Description #}
                        <h4 class="fw-bold text-center mb-3">{{ document.title }}</h4>
                        
                        {% if document.content %}
                        <p class="text-muted text-center mb-3" style="line-height: 1.6;">
                            {{ document.content|striptags|slice(0, 120) }}{% if document.content|striptags|length > 120 %}...{% endif %}
                        </p>
                        {% endif %}

                        {# Document Metadata #}
                        <div class="document-meta text-center mb-4">
                            <div class="row">
                                <div class="col-6">
                                    <small class="text-muted d-block">Version</small>
                                    <span class="fw-bold">{{ document.version }}</span>
                                </div>
                                <div class="col-6">
                                    <small class="text-muted d-block">Mis à jour</small>
                                    <span class="fw-bold">{{ document.updatedAt|date('d/m/Y') }}</span>
                                </div>
                            </div>
                        </div>

                        {# Download Actions #}
                        <div class="text-center">
                            {% if document.hasFile %}
                            <a href="{{ document.fileUrl }}" 
                               target="_blank" 
                               class="btn btn-outline-primary btn-lg mb-2 w-100" 
                               style="border-radius: 25px;">
                                <i class="fas fa-download me-2"></i>Télécharger le PDF
                            </a>
                            {% endif %}
                            
                            <a href="{{ path('app_legal_document_view', {type: document.type}) }}" 
                               class="btn btn-outline-secondary w-100" 
                               style="border-radius: 25px;">
                                <i class="fas fa-eye me-2"></i>Voir en ligne
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        {% else %}
        {# No Documents Available #}
        <div class="row">
            <div class="col-lg-8 mx-auto">
                <div class="card border-0 shadow-sm" style="border-radius: 25px;">
                    <div class="card-body p-5 text-center">
                        <div class="mb-4">
                            <div class="bg-secondary text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 80px; height: 80px;">
                                <i class="fas fa-file-excel fa-2x"></i>
                            </div>
                        </div>
                        <h3 class="fw-bold mb-3">Aucun document disponible</h3>
                        <p class="text-muted mb-4" style="line-height: 1.7;">
                            Aucun document n'est actuellement disponible en téléchargement. 
                            Les documents seront publiés prochainement.
                        </p>
                        <a href="{{ path('app_contact_index') }}" 
                           class="btn btn-primary btn-lg" 
                           style="border-radius: 25px;">
                            <i class="fas fa-envelope me-2"></i>Nous contacter
                        </a>
                    </div>
                </div>
            </div>
        </div>
        {% endif %}

        {# Update Notifications #}
        <div class="row mt-5">
            <div class="col-lg-8 mx-auto">
                <div class="card border-0 shadow-sm" style="border-radius: 25px; background: linear-gradient(135deg, #e8f5e8 0%, #c8e6c9 100%);">
                    <div class="card-body p-4 text-center">
                        <h4 class="fw-bold mb-3">
                            <i class="fas fa-bell me-2"></i>Mises à jour automatiques
                        </h4>
                        <p class="mb-3" style="line-height: 1.6;">
                            Les participants inscrits à nos formations reçoivent automatiquement 
                            les mises à jour de documents par email.
                        </p>
                        <a href="{{ path('app_formations_index') }}" 
                           class="btn btn-success btn-lg" 
                           style="border-radius: 25px;">
                            <i class="fas fa-graduation-cap me-2"></i>Voir nos formations
                        </a>
                    </div>
                </div>
            </div>
        </div>

        {# Help Section #}
        <div class="row mt-5">
            <div class="col-lg-8 mx-auto">
                <div class="card border-0 shadow-sm" style="border-radius: 25px;">
                    <div class="card-body p-4 text-center">
                        <h4 class="fw-bold mb-3">
                            <i class="fas fa-question-circle me-2"></i>Besoin d'aide ?
                        </h4>
                        <p class="text-muted mb-3" style="line-height: 1.6;">
                            Si vous rencontrez des difficultés pour télécharger un document 
                            ou si vous avez des questions, n'hésitez pas à nous contacter.
                        </p>
                        <div class="d-flex flex-wrap gap-3 justify-content-center">
                            <a href="{{ path('app_contact_index') }}" 
                               class="btn btn-outline-primary" 
                               style="border-radius: 25px;">
                                <i class="fas fa-envelope me-2"></i>Contact général
                            </a>
                            <a href="{{ path('app_legal_accessibility') }}" 
                               class="btn btn-outline-info" 
                               style="border-radius: 25px;">
                                <i class="fas fa-universal-access me-2"></i>Accessibilité
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .section-badge {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .document-meta {
            background: rgba(0, 0, 0, 0.05);
            border-radius: 15px;
            padding: 15px;
        }
        
        .card:hover {
            transform: translateY(-5px);
            transition: transform 0.3s ease;
        }
    </style>
{% endblock %}
