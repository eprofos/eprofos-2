<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }} - EPROFOS Étudiant</title>
    
    <!-- Meta tags -->
    <meta name="description" content="Nouveau mot de passe pour la plateforme d'apprentissage EPROFOS étudiants">
    <meta name="robots" content="noindex, nofollow">
    
    <!-- Favicon -->
    <link rel="icon" href="{{ asset('images/favicon.ico') }}">
    
    {{ importmap('private') }}
</head>
<body class="d-flex flex-column">    
    <div class="page page-center">
        <div class="container container-tight py-4">
            <div class="text-center mb-4">
                <a href="{{ path('public_home') }}" class="navbar-brand navbar-brand-autodark">
                    <img src="{{ asset('images/logo.svg') }}" height="36" alt="EPROFOS" onerror="this.style.display='none'; this.nextElementSibling.style.display='block';">
                    <span style="display: none; font-size: 1.5rem; font-weight: bold;">EPROFOS</span>
                </a>
            </div>
            
            <div class="card card-md">
                <div class="card-body">
                    <h2 class="h2 text-center mb-4">Nouveau mot de passe</h2>
                    <p class="text-muted text-center mb-4">
                        Choisissez un nouveau mot de passe sécurisé pour votre compte étudiant.
                    </p>

                    {% for flash_error in app.flashes('error') %}
                        <div class="alert alert-danger" role="alert">
                            <div class="d-flex">
                                <div>
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon alert-icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <circle cx="12" cy="12" r="9"/>
                                        <line x1="12" y1="8" x2="12" y2="12"/>
                                        <line x1="12" y1="16" x2="12.01" y2="16"/>
                                    </svg>
                                </div>
                                <div>
                                    {{ flash_error }}
                                </div>
                            </div>
                        </div>
                    {% endfor %}

                    {% for flash_success in app.flashes('success') %}
                        <div class="alert alert-success" role="alert">
                            <div class="d-flex">
                                <div>
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon alert-icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <circle cx="12" cy="12" r="9"/>
                                        <path d="m9 12 2 2 4 -4"/>
                                    </svg>
                                </div>
                                <div>
                                    {{ flash_success }}
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                    
                    <form method="post" action="{{ path('student_reset_password', {'token': token}) }}" id="reset-password-form">
                        <div class="mb-3">
                            <label class="form-label" for="password">Nouveau mot de passe</label>
                            <div class="input-group input-group-flat">
                                <input type="password" 
                                       class="form-control" 
                                       id="password" 
                                       name="password" 
                                       placeholder="Entrez votre nouveau mot de passe"
                                       required 
                                       minlength="6"
                                       autofocus>
                                <span class="input-group-text">
                                    <a href="#" class="link-secondary" title="Afficher le mot de passe" data-bs-toggle="tooltip" onclick="togglePasswordVisibility('password')">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                            <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                                            <circle cx="12" cy="12" r="2"/>
                                            <path d="M22 12c-2.667 4.667 -6 7 -10 7s-7.333 -2.333 -10 -7c2.667 -4.667 6 -7 10 -7s7.333 2.333 10 7"/>
                                        </svg>
                                    </a>
                                </span>
                            </div>
                            <div class="form-hint">
                                Le mot de passe doit contenir au moins 6 caractères.
                            </div>
                        </div>
                        
                        <div class="mb-3">
                            <label class="form-label" for="confirm_password">Confirmer le mot de passe</label>
                            <div class="input-group input-group-flat">
                                <input type="password" 
                                       class="form-control" 
                                       id="confirm_password" 
                                       name="confirm_password" 
                                       placeholder="Confirmez votre nouveau mot de passe"
                                       required 
                                       minlength="6">
                                <span class="input-group-text">
                                    <a href="#" class="link-secondary" title="Afficher le mot de passe" data-bs-toggle="tooltip" onclick="togglePasswordVisibility('confirm_password')">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                            <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                                            <circle cx="12" cy="12" r="2"/>
                                            <path d="M22 12c-2.667 4.667 -6 7 -10 7s-7.333 -2.333 -10 -7c2.667 -4.667 6 -7 10 -7s7.333 2.333 10 7"/>
                                        </svg>
                                    </a>
                                </span>
                            </div>
                            <div id="password-match-indicator" class="form-hint"></div>
                        </div>
                        
                        <!-- Password Strength Indicator -->
                        <div class="mb-3">
                            <label class="form-label">Force du mot de passe</label>
                            <div class="progress progress-sm">
                                <div id="password-strength" class="progress-bar" role="progressbar" style="width: 0%" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                            <div id="password-strength-text" class="form-hint">Entrez un mot de passe pour voir sa force</div>
                        </div>
                        
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('reset_password') }}">
                        
                        <div class="form-footer">
                            <button type="submit" class="btn btn-primary w-100" id="submit-btn" disabled>
                                <svg xmlns="http://www.w3.org/2000/svg" class="icon me-1" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                    <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                                    <path d="M12 3a12 12 0 0 0 8.5 3a12 12 0 0 1 -8.5 15a12 12 0 0 1 -8.5 -15a12 12 0 0 0 8.5 -3"/>
                                    <circle cx="12" cy="11" r="1"/>
                                    <path d="M12 12l0 2.5"/>
                                </svg>
                                Définir le nouveau mot de passe
                            </button>
                        </div>
                    </form>
                </div>
                
                <!-- Security Tips -->
                <div class="card-footer bg-light">
                    <div class="row align-items-center text-center">
                        <div class="col">
                            <div class="text-muted">
                                <strong>Conseils de sécurité</strong>
                            </div>
                            <div class="mt-2">
                                <a href="{{ path('student_login') }}" class="btn btn-outline-secondary btn-sm">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon me-1" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M9 14l-4 -4l4 -4"/>
                                        <path d="M5 10h11a4 4 0 1 1 0 8h-1"/>
                                    </svg>
                                    Retour à la connexion
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Security Best Practices -->
            <div class="card mt-4">
                <div class="card-body">
                    <h3 class="card-title">
                        <svg xmlns="http://www.w3.org/2000/svg" class="icon me-2" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                            <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                            <path d="M12 3a12 12 0 0 0 8.5 3a12 12 0 0 1 -8.5 15a12 12 0 0 1 -8.5 -15a12 12 0 0 0 8.5 -3"/>
                            <circle cx="12" cy="11" r="1"/>
                            <path d="M12 12l0 2.5"/>
                        </svg>
                        Bonnes pratiques pour votre mot de passe
                    </h3>
                    <div class="row">
                        <div class="col-md-6">
                            <h4 class="text-success">À faire :</h4>
                            <ul class="list-unstyled">
                                <li class="d-flex align-items-center mb-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-sm me-2 text-success" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M5 12l5 5l10 -10"/>
                                    </svg>
                                    <small>Utiliser au moins 8 caractères</small>
                                </li>
                                <li class="d-flex align-items-center mb-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-sm me-2 text-success" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M5 12l5 5l10 -10"/>
                                    </svg>
                                    <small>Mélanger majuscules, minuscules, chiffres et symboles</small>
                                </li>
                                <li class="d-flex align-items-center mb-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-sm me-2 text-success" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M5 12l5 5l10 -10"/>
                                    </svg>
                                    <small>Utiliser un mot de passe unique</small>
                                </li>
                            </ul>
                        </div>
                        <div class="col-md-6">
                            <h4 class="text-danger">À éviter :</h4>
                            <ul class="list-unstyled">
                                <li class="d-flex align-items-center mb-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-sm me-2 text-danger" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M18 6l-12 12"/>
                                        <path d="M6 6l12 12"/>
                                    </svg>
                                    <small>Informations personnelles (nom, date de naissance)</small>
                                </li>
                                <li class="d-flex align-items-center mb-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-sm me-2 text-danger" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M18 6l-12 12"/>
                                        <path d="M6 6l12 12"/>
                                    </svg>
                                    <small>Mots de passe simples (123456, motdepasse)</small>
                                </li>
                                <li class="d-flex align-items-center mb-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-sm me-2 text-danger" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                                        <path stroke="none" d="m0 0h24v24H0z" fill="none"/>
                                        <path d="M18 6l-12 12"/>
                                        <path d="M6 6l12 12"/>
                                    </svg>
                                    <small>Réutiliser d'anciens mots de passe</small>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer footer-transparent d-print-none">
        <div class="container-xl">
            <div class="row text-center align-items-center flex-row-reverse">
                <div class="col-lg-auto ms-lg-auto">
                    <ul class="list-inline list-inline-dots mb-0">
                        <li class="list-inline-item">
                            <a href="#" class="link-secondary">Support</a>
                        </li>
                        <li class="list-inline-item">
                            <a href="#" class="link-secondary">Aide</a>
                        </li>
                        <li class="list-inline-item">
                            <a href="#" class="link-secondary">Conditions d'utilisation</a>
                        </li>
                        <li class="list-inline-item">
                            <a href="#" class="link-secondary">Politique de confidentialité</a>
                        </li>
                    </ul>
                </div>
                <div class="col-12 col-lg-auto mt-3 mt-lg-0">
                    <ul class="list-inline list-inline-dots mb-0">
                        <li class="list-inline-item">
                            © {{ 'now'|date('Y') }} 
                            <a href="{{ path('public_home') }}" class="link-secondary">EPROFOS</a>
                            Tous droits réservés.
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('reset-password-form');
            const submitBtn = document.getElementById('submit-btn');
            const passwordInput = document.getElementById('password');
            const confirmPasswordInput = document.getElementById('confirm_password');
            const passwordStrengthBar = document.getElementById('password-strength');
            const passwordStrengthText = document.getElementById('password-strength-text');
            const passwordMatchIndicator = document.getElementById('password-match-indicator');
            
            function checkPasswordStrength(password) {
                let strength = 0;
                let feedback = [];
                
                if (password.length >= 6) strength += 20;
                else feedback.push('Au moins 6 caractères');
                
                if (password.length >= 8) strength += 10;
                else if (password.length >= 6) feedback.push('8+ caractères recommandés');
                
                if (/[a-z]/.test(password)) strength += 15;
                else feedback.push('Minuscules (a-z)');
                
                if (/[A-Z]/.test(password)) strength += 15;
                else feedback.push('Majuscules (A-Z)');
                
                if (/[0-9]/.test(password)) strength += 15;
                else feedback.push('Chiffres (0-9)');
                
                if (/[^A-Za-z0-9]/.test(password)) strength += 25;
                else feedback.push('Caractères spéciaux (!@#$...)');
                
                return { strength, feedback };
            }
            
            function updatePasswordStrength() {
                const password = passwordInput.value;
                const { strength, feedback } = checkPasswordStrength(password);
                
                passwordStrengthBar.style.width = strength + '%';
                passwordStrengthBar.className = 'progress-bar';
                
                if (strength < 40) {
                    passwordStrengthBar.classList.add('bg-danger');
                    passwordStrengthText.textContent = 'Faible - ' + feedback.join(', ');
                    passwordStrengthText.className = 'form-hint text-danger';
                } else if (strength < 70) {
                    passwordStrengthBar.classList.add('bg-warning');
                    passwordStrengthText.textContent = 'Moyen - ' + feedback.join(', ');
                    passwordStrengthText.className = 'form-hint text-warning';
                } else {
                    passwordStrengthBar.classList.add('bg-success');
                    passwordStrengthText.textContent = 'Fort - Excellent choix !';
                    passwordStrengthText.className = 'form-hint text-success';
                }
                
                validateForm();
            }
            
            function validatePasswordMatch() {
                const password = passwordInput.value;
                const confirmPassword = confirmPasswordInput.value;
                
                if (!confirmPassword) {
                    passwordMatchIndicator.textContent = '';
                    passwordMatchIndicator.className = 'form-hint';
                } else if (password === confirmPassword) {
                    passwordMatchIndicator.textContent = '✓ Les mots de passe correspondent';
                    passwordMatchIndicator.className = 'form-hint text-success';
                } else {
                    passwordMatchIndicator.textContent = '✗ Les mots de passe ne correspondent pas';
                    passwordMatchIndicator.className = 'form-hint text-danger';
                }
                
                validateForm();
            }
            
            function validateForm() {
                const password = passwordInput.value;
                const confirmPassword = confirmPasswordInput.value;
                const { strength } = checkPasswordStrength(password);
                
                const isValid = password.length >= 6 && 
                              password === confirmPassword && 
                              strength >= 40;
                
                submitBtn.disabled = !isValid;
            }
            
            passwordInput.addEventListener('input', function() {
                updatePasswordStrength();
                validatePasswordMatch();
            });
            
            confirmPasswordInput.addEventListener('input', validatePasswordMatch);
            
            form.addEventListener('submit', function(e) {
                if (submitBtn.disabled) {
                    e.preventDefault();
                    return;
                }
                
                // Disable submit button to prevent double submission
                submitBtn.disabled = true;
                submitBtn.innerHTML = `
                    <div class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></div>
                    Définition en cours...
                `;
            });
        });
        
        function togglePasswordVisibility(inputId) {
            const input = document.getElementById(inputId);
            const icon = event.currentTarget.querySelector('svg');
            
            if (input.type === 'password') {
                input.type = 'text';
                icon.innerHTML = `
                    <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                    <path d="M21 9c-2.4 2.667 -5.4 4 -9 4c-3.6 0 -6.6 -1.333 -9 -4"/>
                    <path d="M3 3l18 18"/>
                    <path d="M10.584 10.587a2 2 0 0 0 2.828 2.83"/>
                `;
            } else {
                input.type = 'password';
                icon.innerHTML = `
                    <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                    <circle cx="12" cy="12" r="2"/>
                    <path d="M22 12c-2.667 4.667 -6 7 -10 7s-7.333 -2.333 -10 -7c2.667 -4.667 6 -7 10 -7s7.333 2.333 10 7"/>
                `;
            }
        }
    </script>
</body>
</html>
